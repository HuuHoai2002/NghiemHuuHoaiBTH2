@model NghiemHuuHoaiBTH2.Models.Employee

@{
  ViewData["Title"] = "Edit";
}

<h4 class="py-5">Sửa thông tin Employee</h4>

<div class="row">
  <div class="col-md-4">
    <form method="post" asp-action="Edit">
      <span asp-validation-sumary="ModelOnly"></span>
      <input type="hidden" value="@Model.EmployeeID" asp-for="EmployeeID" />
      <div class="form-group">
        <label asp-for="EmployeeName" class="control-label">Name</label>
        <input asp-for="EmployeeName" class="control-label" value="@Model.EmployeeName" />
        <span asp-validation-for="EmployeeName" class="text-danger"></span>
      </div>
      <div class="form-group">
        <input type="submit" value="Cập nhật" class="btn btn-primary" /> |
        <a class="btn btn-success" asp-action="Index">Danh sách Employee</a>
      </div>
    </form>
  </div>
</div>

@section Scripts {
@{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}